import styled from "styled-components";

const Img = styled.img`
  display: block;
`;

const Header = () => (
  <header>
    <Img src="static/headerSub.gif" alt="bestdestinations" />
  </header>
);

export default Header;
